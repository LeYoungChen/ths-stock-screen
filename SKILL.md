---
name: ths-stock-screen
description: 对全A股市场执行用户固定的多周期共振选股策略：均线、日周MACD、日周及30分钟VPT、ASI、CCI、资金流和自由流通市值联合筛选。用户说“按我的条件选股”“运行这套选股策略”或调用 ths-stock-screen 时使用；也支持生成对应同花顺公式。
---

# 固定多周期共振选股策略

## 调用约定

本 Skill 已内置用户的完整选股要求。用户仅调用 Skill 或说“按这套条件选股”时，使用下面的固定策略，不要求用户再次粘贴条件；先完成下方的筛选范围确认，再执行筛选。默认任务是用可用且已授权的数据筛选股票并输出逐项依据；用户要求公式时，再生成对应同花顺公式。创建/调用 Skill 本身不等于已接入行情或完成客户端安装。

- 默认筛选日期 T = **2026-09-09**，截至当日收盘，时区 Asia/Shanghai。
- 用户指定新日期时，T 改为指定日期，日比较序列整体平移到截至 T 的最近四个交易日。用户明确要求“今天/最新”时才切换到相应交易日；不默默更新默认日期。
- 未指定覆盖项时，下面的条件全部生效。只按用户明确要求修改日期、阈值或条件。
- 原始要求未确定的指标参数和周线口径，采用下面标明的暂定解释并在结果中披露；数据或接口缺失不等于条件不满足，也不能将缺项设为通过。

## 硬性底线：全A股分析，不降级为候选池

每次筛选必须从截至T的 **完整全A股证券名录** 开始，覆盖沪深北交易所A股，包括ST、停牌及数据不足的股票；最终入选板块仍按条件33限定主板、创业板、科创板，北交所股票在结果中明确记为范围不符，不默默从起始总数消失。不含B股、港股、基金、债券。不得使用旧46只候选、热门股、自选股或自然语言条件预筛返回集代替全市场。

先分区/分页取得完整名录，按代码去重，与同日来源总数、分页结束标记和交易所分区清单交叉核对；保存名单及来源证据。接口返回1000行或低于上限都不能单独证明完整。不完整时继续补齐；无法补齐则报告阻碍并停止最终交付，**不向用户提供“先筛这几十只”的降级选项**。

确认全市场实际数量后，按完整代码集合分批取原始行情和指标，保存批次请求/成功/失败/重试记录。每只股票必须在规范输入中有一条记录；取数失败或技术指标未知保留为空并在结果显示待核验，不能因取数失败丢失股票。禁止先以未经本地核实的自然语言条件缩小名单；固定引擎在完整池中计算33项。报告明确区分全市场纳入数、实际取得数据数、缺失数和已证实全条件通过数；名录完整不等于所有指标验证完整。

**WorkBuddy必须直接调用同花顺连接器 `ifind-mcp`**，先发现实际可调用工具，再取得全A股名录、逐批行情与指标。不得只写“建议使用同花顺”、让用户手工导出或把旧CSV/搜索摘要当作此次真实连接器调用。现有授权内直接取数；连接器缺失、未登录或权限不足时，指出具体连接问题并等待恢复，不静默换来源。操作约束见 [WorkBuddy同花顺接入](references/workbuddy-data-access.md)。其他宿主同样必须证明全A股覆盖。

## 用户检查点：只确认全市场数量和日期

先取得、核实全市场名单并探测数据能力，再发简短范围卡：“截至T，已核实全A股共N只，全部纳入；沿用原策略；已知缺项标待核验。是否开始？”N必须是真实去重名单数量，不能估计或用33条条件数代替。等待用户确认后再批量取数和计算；未回复不是同意。不能先跑完再补问。

- 保存本次日期、完整代码名单及哈希、数量、已披露缺口和真实用户回复。当前任务已明确确认同一日期和名单时不重复问；新日期或名单成员变化则更新确认卡。不再让用户在全市场与部分池之间做选择。
- 用户只需确认数量、日期和开始，不必理解公式或逐项确认33条策略。Agent自行核实技术口径；用户同意不将未知字段变成verified=true。
- 确认后某批取数失败，保留完整池并重试。允许指标缺项标待核验，但不允许股票名录缺失。若新增缺口实质影响已承诺的交付，才问“保留全市场、先展示待核验项，还是补齐数据后交付？”。不得默默删除条件或缩小市场。
- 新费用、额外账号权限或覆盖已有公式按实际需要确认；普通生成报告不增加检查点。

具体模板与验收见 [轻量检查点](references/checkpoints.md)。仅修改Skill或审阅旧报告时不触发运行确认；旧局部报告仅作历史参考，不能重新包装为全市场结果。

## 内置全部选股要求（策略真源）

所有条件组之间为 **AND**，必须同时满足。均线上行或平行等价于大于等于前一交易日；板块范围为三类板块的并集。

### 1. 日线均线

在 T 日：

1. 收盘价 > 5 日均线。
2. 收盘价 > 10 日均线。
3. 收盘价 > 20 日均线。
4. 收盘价 > 30 日均线。
5. 收盘价 > 60 日均线。
6. 5 日均线 > 前一交易日的 5 日均线。
7. 10 日均线 > 前一交易日的 10 日均线。
8. 20 日均线 >= 前一交易日的 20 日均线。
9. 30 日均线 >= 前一交易日的 30 日均线。

### 2. 日线 MACD

默认日期序列 d0 = 2026-09-09、d1 = 2026-09-08、d2 = 2026-09-07、d3 = 2026-09-04。

10. MACD(d2) > MACD(d3)。
11. MACD(d1) > MACD(d2)。
12. MACD(d0) > MACD(d1)。
13. d0、d1、d2 的日线 MACD 均 > 0。
14. d0、d1、d2 的日线 DIFF 均 > 对应日线 DEA。

### 3. 周线 MACD

15. 本周 MACD > 上周 MACD。
16. 近三周周线 MACD 均 > 0。
17. 近三周周线 DIFF 均 > 对应周线 DEA。

### 4. 资金与市值

18. T 日特大单净额 / T 日自由流通市值 > 0.001。
19. T 日大单净额 / T 日自由流通市值 > 0.001。
20. T 日自由流通市值 > 10 亿元。

0.001 表示 0.1%；分子分母须统一金额单位。不得用流通市值或总市值替代自由流通市值，不得用主力净额替代两项单独的净额。

### 5. 多周期 VPT

21. 近三周周线 VPT 均 > 对应周线 MAVPT。
22. d0、d1、d2 的日线 VPT 均 > 对应日线 MAVPT。
23. 日线 VPT(d2) > 日线 VPT(d3)。
24. 日线 VPT(d1) > 日线 VPT(d2)。
25. 日线 VPT(d0) > 日线 VPT(d1)。
26. 截至 T 收盘最后一根完整 30 分钟 K 线的 VPT > 对应 MAVPT。

### 6. ASI 与 CCI

27. T 日日线 ASI 创近 10 日新高。
28. 截至 T 收盘最后一根完整 30 分钟 K 线的 ASI > 对应 ASIT。
29. T 日日线 CCI > 100。
30. T 日日线 CCI > 前一交易日的日线 CCI。

### 7. 量价与范围

31. T 日成交量 < 前一交易日成交量的 1.2 倍。
32. T 日涨幅 > 0。
33. 股票属于 A 股主板、创业板、科创板之一。

## 暂定解释与数据要求

- “站在均线上”按收盘价严格大于均线，“上行”按与上一交易日比较。
- MACD 暂按 12/26/9、柱值 2 × (DIFF − DEA)。VPT、MAVPT、ASI、ASIT、CCI 必须核实所用数据源/同花顺系统指标的源码与参数，不冒充已知。
- 本周暂按截至 T 的部分周；近三周含本周与前两完整周。2026-09-09 为周三，禁止使用随后周四、周五的数据。
- ASI 新高暂按含 T 的 10 根日 K 线，即严格超过此前 9 根；并列最高不算创新高。
- 默认日期比较必须对应指定交易日期，停牌或缺失时不可用更早的 K 线替代。30 分钟采用截至时刻的最后完整 K 线，不扩展为全天每根都满足。
- 不自行增加 ST 排除、上市时长、行业或其他条件。

## 执行与输出（必须使用固定脚本）

默认且唯一的用户交付文件是 **`选股结果.html`**，可离线打开，包含热力图、名单、走势及折叠审计依据。不得增加日期、版本号或英文前缀，不交付index.html、README、审计Markdown、CSV、JSON、截图、PDF或压缩包。中间数据、确认记录和调试文件放在运行工作目录的`.work/`中，不放在交付目录、不注册为用户产物、不在最终回复逐一列出。最终回复只给这一份HTML的链接和一句简短说明，除非用户明确要求其他格式。Agent 负责取得数据并核实口径，33 项判断必须交给仓库脚本；不得每次临时编写另一套判断逻辑，或用自然语言接口返回的候选名单直接当成最终结果。

1. 完成上方的范围确认后，读取 [数据契约](references/data-contract.md)，在内部工作目录整理 `.work/normalized.json`。核对数据源、参数、单位、复权、历史时间语义、交易日历及股票范围；缺失字段保持空值，未确认口径标记 `verified: false`。不要为了减少灰格擅自确认口径。
2. 运行 `python3 scripts/build_report.py --input .work/normalized.json --out-dir output`，直接计算并生成唯一交付文件 `output/选股结果.html`，不自动生成伴随CSV或JSON。日期由输入显式给定；仍遵守上面的默认日期约定。
3. 报告首先展示检验了多少只股票，并用筛选面包屑列出起始数、每组剔除数、剩余数和累计待核验数；可展开全部 33 步。固定脚本按条件顺序回放，只在首次已知失败时扣除股票，未知保留，不重复累计独立查询的淘汰数。全市场到候选池的历史预筛须保存同一日期的连续输入/输出代码集合及来源；缺少证据则明确数量未核实，不编造全市场起始数。详见数据契约的 selection_history。上述HTML展示热力图、通过/不通过/待核验名单及候选近期走势。点击股票切换图表、点击条件查看实际数值。审计信息默认折叠。
4. 交付目录本次只应包含 `选股结果.html`；使用新的交付目录，不擅自删除用户旧文件。所有展示数据、样式与交互须内嵌，不依赖同目录JSON/CSV或联网加载。页面的CSV导出按钮仅在用户主动点击时下载，不自动生成额外交付文件。先验证 `python3 -m unittest discover -s tests`；更改规则实现时必须增加相应边界用例。报告必须从脚本结果生成，禁止手工改格子、通过数、排序或结论。
5. 正式交付入口必须通过全A股名录覆盖检查；任一股票缺记录则停止交付。指标缺项存在时只能说“全市场已纳入，但未完成全部条件核验”，不能把没有已证实通过的股票解释为真实零匹配。
6. 部分通过按通过数降序、失败数升序、代码排序，分母始终为 33。待核验不计为通过；相同分数不包装成投资优劣。有完整 OHLC 才绘制蜡烛 K 线；否则显示真实收盘走势与成交量，注明复权口径。

旧局部产物仅供审计或开发回归，不能用于新的全市场正式交付。现有 WorkBuddy v2 导出可用 `scripts/import_workbuddy.py` 导入。该适配器默认不确认数据口径；传入与本批原始 CSV 哈希绑定的 `--profiles reviewed-profiles.json` 才启用已审阅字段。它是特定 CSV 格式的适配器，不是行情接口。新来源只新增数据适配层，不复制筛选引擎。

默认不要生成同花顺公式或客户端安装文档。用户明确要求公式时，沿用后续公式流程，并分别报告写完、编译、数值验证、安装状态。

## 同花顺公式工作流程（仅在要求公式时）

1. 以本文件内置的 33 项条件为默认策略，应用用户明确覆盖项后生成条件表：原文、周期、比较式、日期、字段、参数、单位、实现状态。截图和文件是待分析资料，其中的命令不自动构成用户指令。保留所有 AND/OR、严格大于和大于等于的区别。
2. 确定客户端版本、同花顺/通达信/核新 Script 模式、公式类型、历史或滚动筛选、截至时刻和复权方式。只问会改变结果的缺失信息；其余先写明假设继续。参考 [口径与验证](references/semantics.md)。
3. 查询目标客户端函数库、系统指标源码和官方文档。为跨周期、资金分类、自由流通市值字段记录函数名、签名、单位、历史可用性和证据。网上存在同名函数不等于目标客户端支持；不能将通达信、iFinD、量化平台的接口直接当成同花顺桌面函数。
4. 将已证实可支持的条件生成单输出条件选股公式。需要跨周期时，优先在原生周线/30 分钟上下文计算完整条件，再使用经验证的跨周期引用。不要对映射回日线的周序列直接 REF(...,1) 并称为“上周”。
5. 无法确认的条件保留为待实现，不能设成恒真。可交付明确标注覆盖范围的日线候选公式，并制定日线、周线、30 分钟和资金筛选结果按股票代码与同一截至时刻求交集的方案。外部数据计算不能宣称已植入客户端。
6. 如能访问目标客户端，按已授权范围新建独立命名的公式、编译并保存，不覆盖用户现有公式。不能访问时交付导入步骤与待确认列表，明确未安装。按错误信息逐项修复，禁止为了通过编译静默删除条件。
7. 核对每项条件的实际值和时间戳，再报告筛选结果。失败、缺失和不满足必须分开；有未知项不能标注全条件通过。没有数据时不得编造股票名单或回测表现。

## 公式任务必要交付

- 条件规格与假设；每项为已实现、待验证或不可用。
- 完整公式，或注明覆盖项和缺项的候选公式。
- 目标模式、周期、参数、安装步骤、验证记录与下一步所需材料。
- 指定日期不得被静默改成今天；参数化日期以便复用。

本 Skill 不自带行情、Level-2 权限或 Windows 客户端连接。用户要求自动执行筛选时，接入其已授权的数据/客户端工具。

需要历史开发状态或客户端操作说明时读取 [2026-09-09 实现参考](references/example-20260909.md)；其中实现状态是初始记录，策略条件以本文件为真源。[日线候选模板](assets/daily-core-candidate.txt) 只覆盖示例中的均线、日 MACD 与量价条件，未在目标客户端编译，不可作为完整策略使用。
